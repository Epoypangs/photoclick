using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pnaelshow : MonoBehaviour
{
    // Start is called before the first frame update

    public GameObject show;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void jerald()
    {
        show.SetActive(true);
    }
}
